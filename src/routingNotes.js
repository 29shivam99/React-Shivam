// we will use react router librabry
// npm i react-router-dom

//! types of routing -

// client side routing  - which we do in SPA, instead of loading/network pages we use components which justchanges their position
// server side routing - earlier days we used <a href> and when clicking Netwrok call made and fecthes the new html page which was targetted
